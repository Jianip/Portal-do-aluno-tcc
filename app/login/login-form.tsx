"use client";import {useActionState} from "react";import {login} from "./actions";import {Loader2,LogIn} from "lucide-react";
export function LoginForm(){const [state,action,pending]=useActionState(login,{});return <form action={action} className="space-y-4"><label className="block text-sm font-bold">E-mail<input className="input mt-2" name="email" type="email" defaultValue="aluno@escolafuturo.edu.br" required/></label><label className="block text-sm font-bold">Senha<input className="input mt-2" name="password" type="password" defaultValue="Portal@123" required/></label>{state.error&&<p role="alert" className="rounded-xl bg-red-50 p-3 text-sm text-red-700">{state.error}</p>}<button disabled={pending} className="btn btn-primary w-full">{pending?<Loader2 className="animate-spin" size={18}/>:<LogIn size={18}/>}Entrar no portal</button></form>}
