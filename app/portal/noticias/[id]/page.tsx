import Image from "next/image";import Link from "next/link";import {notFound} from "next/navigation";import {ArrowLeft} from "lucide-react";import {news} from "@/lib/demo-data";import {formatDate} from "@/lib/utils";
export default async function Noticia({params}:{params:Promise<{id:string}>}){const {id}=await params,n=news.find(x=>x.id===id);if(!n)notFound();return <article className="mx-auto max-w-4xl"><Link href="/portal/noticias" className="mb-6 flex items-center gap-2 font-bold text-blue-600"><ArrowLeft size={18}/>Voltar</Link><span className="badge">{n.category}</span><h1 className="mt-4 text-4xl font-black md:text-6xl">{n.title}</h1><p className="muted mt-5">Por {n.author} · {formatDate(n.date)}</p><div className="relative my-8 h-64 overflow-hidden rounded-3xl md:h-[440px]"><Image src={n.image} alt="" fill className="object-cover"/></div><div className="text-lg leading-8"><p className="font-bold">{n.summary}</p><p className="muted mt-5">A conquista celebra o compromisso dos estudantes, o acompanhamento dos professores e uma cultura escolar que valoriza a curiosidade. O resultado é motivo de orgulho para toda a comunidade.</p><p className="muted mt-5">Nas próximas semanas, novas atividades serão realizadas para compartilhar aprendizados e preparar os alunos para os desafios seguintes.</p></div></article>}
