import Image from "next/image";import Link from "next/link";import {ArrowRight} from "lucide-react";import {news} from "@/lib/demo-data";import {PageTitle} from "@/components/page-title";import {SearchFilter} from "@/components/search-filter";import {formatDate} from "@/lib/utils";
export default function Noticias(){const featured=news[0];return <><PageTitle eyebrow="Fique por dentro" title="Notícias" description="Histórias, conquistas e novidades da nossa comunidade."/><SearchFilter placeholder="Buscar notícias..." categories={["Conquistas","Institucional","Comunicados"]}/><Link href={`/portal/noticias/${featured.id}`} className="card mb-7 grid overflow-hidden md:grid-cols-2"><div className="relative min-h-64"><Image src={featured.image} alt="" fill className="object-cover"/></div><div className="flex flex-col justify-center p-7"><span className="badge w-fit">Em destaque · {featured.category}</span><h2 className="mt-4 text-3xl font-black">{featured.title}</h2><p className="muted mt-3">{featured.summary}</p><p className="mt-6 text-sm font-bold text-blue-600">Ler notícia <ArrowRight className="inline" size={16}/></p></div></Link><div className="grid gap-6 md:grid-cols-2">{news.slice(1).map(n=><Link href={`/portal/noticias/${n.id}`} key={n.id} className="card overflow-hidden"><div className="relative h-52"><Image src={n.image} alt="" fill className="object-cover"/></div><div className="p-5"><span className="badge">{n.category}</span><h2 className="mt-3 text-xl font-black">{n.title}</h2><p className="muted mt-2 text-sm">{n.summary}</p><p className="muted mt-4 text-xs">{n.author} · {formatDate(n.date)}</p></div></Link>)}</div></>}
