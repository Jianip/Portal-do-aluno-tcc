"use client";import {Download,Share2,Smartphone,X} from "lucide-react";import {useEffect,useState} from "react";
interface InstallPromptEvent extends Event{prompt:()=>Promise<void>;userChoice:Promise<{outcome:"accepted"|"dismissed"}>}
export function InstallApp(){const[prompt,setPrompt]=useState<InstallPromptEvent|null>(null),[ios,setIos]=useState(false),[visible,setVisible]=useState(false);useEffect(()=>{const standalone=window.matchMedia("(display-mode: standalone)").matches||("standalone" in navigator&&(navigator as Navigator&{standalone?:boolean}).standalone);if(standalone)return;setIos(/iphone|ipad|ipod/i.test(navigator.userAgent));const handler=(event:Event)=>{event.preventDefault();setPrompt(event as InstallPromptEvent);setVisible(true)};window.addEventListener("beforeinstallprompt",handler);if(/iphone|ipad|ipod/i.test(navigator.userAgent))setVisible(true);return()=>window.removeEventListener("beforeinstallprompt",handler)},[]);async function install(){if(!prompt)return;await prompt.prompt();const result=await prompt.userChoice;if(result.outcome==="accepted")setVisible(false);setPrompt(null)}if(!visible)return null;return <section className="card relative mb-7 overflow-hidden border-blue-200 bg-gradient-to-r from-blue-50 to-violet-50 p-5 dark:from-blue-950/50 dark:to-violet-950/50"><button onClick={()=>setVisible(false)} className="absolute right-3 top-3 rounded-lg p-2 muted" aria-label="Fechar"><X size={17}/></button><div className="flex flex-col gap-4 pr-8 sm:flex-row sm:items-center"><span className="flex h-12 w-12 shrink-0 items-center justify-center rounded-2xl bg-gradient-to-br from-blue-600 to-violet-600 text-white"><Smartphone/></span><div className="flex-1"><h2 className="font-black">Instale o Portal Sanico Teles</h2><p className="muted mt-1 text-sm">Acesse rapidamente pela tela inicial do seu celular.</p></div>{ios?<div className="rounded-xl border bg-white/70 px-4 py-3 text-sm dark:bg-slate-900/70"><Share2 className="mr-2 inline text-blue-600" size={17}/>Toque em <b>Compartilhar</b> e depois em <b>Adicionar à Tela de Início</b>.</div>:<button onClick={install} className="btn btn-primary"><Download size={17}/>Instalar aplicativo</button>}</div></section>}
