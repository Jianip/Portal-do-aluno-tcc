"use client";
import {useActionState,useEffect,useRef,useState} from "react";
import {Eye,EyeOff,Loader2,Lock,X} from "lucide-react";
import {toast} from "sonner";
import {changePassword} from "@/app/portal/perfil/actions";
// The conditional toast expression selects the correct user feedback variant.
// eslint-disable-next-line @typescript-eslint/no-unused-expressions
export function ChangePassword(){const[open,setOpen]=useState(false),[show,setShow]=useState(false);const[state,action,pending]=useActionState(changePassword,{});const formRef=useRef<HTMLFormElement>(null);useEffect(()=>{if(!state.message)return;state.status==="success"?toast.success(state.message):toast.error(state.message);if(state.status==="success"){formRef.current?.reset();setOpen(false)}},[state]);return <><button onClick={()=>setOpen(true)} className="btn border"><Lock size={17}/>Alterar senha</button>{open&&<div className="fixed inset-0 z-50 flex items-center justify-center bg-slate-950/55 p-4 backdrop-blur-sm" role="dialog" aria-modal="true" aria-labelledby="password-title" onMouseDown={e=>{if(e.target===e.currentTarget)setOpen(false)}}><div className="card w-full max-w-md p-6"><div className="flex items-start justify-between"><div><h2 id="password-title" className="text-2xl font-black">Alterar senha</h2><p className="muted mt-1 text-sm">Use uma senha forte e diferente da atual.</p></div><button onClick={()=>setOpen(false)} className="rounded-lg p-2 hover:bg-black/5" aria-label="Fechar"><X size={20}/></button></div><form ref={formRef} action={action} className="mt-6 space-y-4"><label className="block text-sm font-bold">Senha atual<input name="currentPassword" type={show?"text":"password"} className="input mt-2" required autoComplete="current-password"/></label><label className="block text-sm font-bold">Nova senha<input name="newPassword" type={show?"text":"password"} className="input mt-2" required minLength={8} autoComplete="new-password"/></label><label className="block text-sm font-bold">Confirmar nova senha<input name="confirmPassword" type={show?"text":"password"} className="input mt-2" required minLength={8} autoComplete="new-password"/></label><button type="button" onClick={()=>setShow(v=>!v)} className="muted flex items-center gap-2 text-sm">{show?<EyeOff size={16}/>:<Eye size={16}/>} {show?"Ocultar senhas":"Mostrar senhas"}</button>{state.status==="error"&&<p role="alert" className="rounded-xl bg-red-50 p-3 text-sm text-red-700">{state.message}</p>}<div className="flex justify-end gap-3 pt-2"><button type="button" onClick={()=>setOpen(false)} className="btn border">Cancelar</button><button disabled={pending} className="btn btn-primary">{pending&&<Loader2 className="animate-spin" size={17}/>}Salvar nova senha</button></div></form></div></div>}</>}
